# The live suite talks to the real Beatport API and needs credentials in
# BEATPORT_USERNAME and BEATPORT_PASSWORD. Excluded from the default run:
#   flutter test                          unit tests only
#   flutter test --tags live              include the live suite
tags:
  live:
    skip: false
